import 'package:{{packageName}}/core/core.dart';
import 'package:{{packageName}}/features/features.dart';

/// Placeholder auth screen — wire real login / session here.
class AuthPlaceholderView extends StatelessWidget {
  const AuthPlaceholderView({super.key});

  @override
  Widget build(BuildContext context) {
    return BaseScaffold(
      useSingleScroll: true,
      bg: Palette.bg,
      body: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          context.verticalSpace(48),
          TextWidget(
            'Auth',
            fontSize: 28,
            fontWeight: Weight.w700,
            textColor: Palette.textPrimary,
          ),
          context.verticalSpace(8),
          TextWidget(
            'Secure storage is registered in GetIt. Replace this screen.',
            textColor: Palette.textMuted,
          ),
        ],
      ),
    );
  }
}
